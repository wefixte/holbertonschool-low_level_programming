#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @num: number to return
 *
 * Return: the absolute value
 */

int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
